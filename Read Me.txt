If for various problems the “run.bat” file does not work open a terminal inside the folder (right click and then open terminal) and type inside “python3 main.py”
----
before starting “clone.bat” start “setup.bat”, to install all the libraries needed to use the tool 
----
this tool was created for fun, I take no responsibility if this tool will create damage etc.